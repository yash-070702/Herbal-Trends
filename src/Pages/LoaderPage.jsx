import VeterinaryLoader from "../components/Loader";
export default function Page() {
  return <VeterinaryLoader 
  variant="rotating" 
  size="lg"
  text="Preparing herbal care data..."
  fullScreen={true}
/>
}